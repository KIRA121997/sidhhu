- name: variable playbook 
  hosts: localhost
  vars:
    - URL: "www.com.com"
  tasks:
    - name: print url1
      debug:
      msg: "{{ URL}}"

    - name: print url2
      debug:
        msg: "{{ URL}}"  